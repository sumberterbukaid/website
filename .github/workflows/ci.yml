name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    name: Build & verifikasi
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build (astro + pagefind)
        run: npm run build
